---
name: DOMAIN-arsenal
description: >
  One line: the tool arsenal for <domain> and its trigger signals (ports, service types,
  phase). Authorized targets only.
domain: web
type: arsenal
stability: locked
modes: [pentest, bugbounty]
tools: []
schema_version: 1
---

# <domain> — tool arsenal

Group by sub-task. For each tool: **name** — one-line why-this-tool-over-alternatives,
then the exact command with a gloss of the flags that matter, then a Gotcha.

## 1. <sub-task>
**tool** — why it's the default.
`exact command` — `-flag` = what it does.
Gotcha: the common trap.
